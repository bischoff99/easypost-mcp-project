# EasyPost Test Environment Configuration
# ===========================================
# This file is loaded when ENVIRONMENT=test
# Safe for development and testing - no real charges

# ============================================================================
# EasyPost API Configuration (TEST MODE)
# ============================================================================
# Get your TEST API key from: https://www.easypost.com/account/api-keys
# Test keys start with EZTK... (not EZAK...)
# ✓ API key loaded from macOS Keychain (see .envrc)
# To set: security add-generic-password -s "easypost-test" -a "${USER}" -w "YOUR_KEY"
# EASYPOST_API_KEY is loaded from keychain via .envrc

# ============================================================================
# Environment
# ============================================================================
ENVIRONMENT=test

# ============================================================================
# Server Configuration
# ============================================================================
MCP_HOST=0.0.0.0
MCP_PORT=8000
MCP_LOG_LEVEL=DEBUG

# ============================================================================
# Database Configuration (Test Database)
# ============================================================================
DATABASE_URL=postgresql+asyncpg://easypost:easypost@localhost:5432/easypost_mcp_test

# Database Connection Pool Settings
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=5
DATABASE_POOL_RECYCLE=1800
DATABASE_POOL_TIMEOUT=10
DATABASE_COMMAND_TIMEOUT=60
DATABASE_CONNECT_TIMEOUT=10
DATABASE_STATEMENT_TIMEOUT_MS=15000

# ============================================================================
# CORS Configuration
# ============================================================================
CORS_ORIGINS=http://localhost:5173,http://localhost:4173,http://localhost:3000
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS

# ============================================================================
# Performance Settings
# ============================================================================
MAX_BULK_CONCURRENCY=16

# ============================================================================
# Debug Settings (Enabled for test environment)
# ============================================================================
DEBUG=true
