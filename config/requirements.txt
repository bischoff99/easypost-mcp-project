# Core Framework (pinned major versions for stability)
fastmcp>=2.0.0,<3.0.0
fastapi>=0.100.0,<0.122.0
easypost>=10.0.0,<11.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.25.0,<1.0.0
pydantic>=2.5.0,<3.0.0
uvicorn>=0.24.0,<1.0.0
starlette>=0.35.0,<1.0.0
aiofiles>=23.2.1,<26.0.0

# Testing (pinned for reproducibility)
pytest>=7.4.3,<8.0.0
pytest-asyncio>=0.21.1,<2.0.0
pytest-cov>=4.0.0,<5.0.0
pytest-watch>=4.2.0,<5.0.0
pytest-xdist>=3.5.0,<4.0.0

# Code Quality (allow minor updates)
ruff>=0.1.0,<1.0.0
black>=23.0.0,<26.0.0

# Note: Database dependencies removed for personal use (YAGNI)
# All data fetched directly from EasyPost API on-demand

# Note: Run `pip install -r requirements.txt` to install
# For exact versions: `pip freeze > requirements.lock`
# Consider using `pip-tools` or `uv` for better dependency management
