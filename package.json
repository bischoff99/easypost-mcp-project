{
  "name": "easypost-mcp-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/frontend"
  ],
  "scripts": {
    "dev": "echo \"Run per-app: cd apps/frontend && pnpm run dev\"",
    "build": "echo \"Run per-app: cd apps/frontend && pnpm run build\"",
    "test": "echo \"Run per-app tests\"",
    "lint": "echo \"Run per-app linters\""
  }
}